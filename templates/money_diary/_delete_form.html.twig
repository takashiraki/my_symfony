<form method="post" action="{{ path('app_money_diary_delete', {'id': money_diary.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ money_diary.id) }}">
    <button class="btn">Delete</button>
</form>
