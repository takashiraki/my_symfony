# VCS
.git
.gitignore

# Local env / secrets (never bake into an image)
.env.local
.env.*.local

# Dev databases / user data
migrations/*.db

# Dependencies (installed during build)
vendor/
node_modules/

# Dev tooling / caches
var/
.php-cs-fixer.cache
.idea/
.vscode/
.claude/
.devcontainer/
tests/
